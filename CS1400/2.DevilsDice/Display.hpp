#ifndef DISPLAY_HPP
#define DISPLAY_HPP
namespace //annonymous name space, can only be used inside of this .cpp file, and it wont conflict with anything
{
	const auto INCREMENT = 5;
	const auto WINNING_SCORE = 100;
}
void displayDie(int Value1);
void display(int player1Score, int devilScore);

#endif
